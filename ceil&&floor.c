#include<stdio.h>
#include<math.h>

int main()
{

    int x, y;
    float p, q;
    p= 1.9;
    q = 5.8;
    printf("The ceil of %.2f is %f\n", ceil(p));// the work of ceil function is to go to the upper nearest float number
    printf("The floor of %.2f is %f\n", floor(q));// the work of floor function is to go to the lower nearest float number
    return 0;
}
